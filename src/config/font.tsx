import { Chela_One, Modak, Potta_One } from "next/font/google";

export const font_title = Potta_One({ subsets: ['latin'], weight: '400' })
export const font_header = Modak({ subsets: ['latin'], weight: '400' })

